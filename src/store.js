import { atom } from 'nanostores';

export const updateValuesModal = atom(false);
export const updateValuesList = atom([]);
export const currentValuesList = atom([]);
export const circlesToSpawn = atom([])